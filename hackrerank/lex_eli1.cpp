#include<iostream>
using namespace std;
int a[26];
int main()
{
    string s,ss;
    //cin>>s;
    /*int m=0;
    char ch;
    for(int i=0;i<s.size();i++)
        a[s[i]-'a']++;
    for(int i=0;i<26;i++)
    {
        if(a[i]==0)
        {
            m=i;
            break;
        }
    }
    ch='a'+m;
        cout<<ch;*/
    //ss=prev_permutation(s.begin(),s.end())
      if('ab'>'c')
            cout<<"yes";
      else
        cout<<"no";
    return 0;
}
